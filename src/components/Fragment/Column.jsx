import React from 'react'

function Column() {
  return (
    <React.Fragment>
        {/* {items.map(items => (
            <React.Fragment key ={item.id}>
                <h1>{item.title}</h1>
                <p>{item.paragraph}</p>
                </React.Fragment>
        )
        )} */}
      <td>Name</td>
      <td>Vishwas</td>
    </React.Fragment>
  )
}

export default Column
